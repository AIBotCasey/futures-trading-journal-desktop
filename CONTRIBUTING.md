# Contributing

Thanks for helping.

## Dev setup
1. Install prerequisites (Node + Rust + Tauri prerequisites)
2. Run:
   ```bash
   cd apps/desktop
   npm install
   npm run tauri dev
   ```

## Pull requests
- Keep changes focused.
- Add/adjust docs when behavior changes.
- No telemetry/network calls without explicit discussion.
